# Core configuration properties for IntelliJ compatibility
# These ensure the application starts even if environment variables are not set

# Base directory for generated projects
codegen.output-base-dir=./generated-projects

# Template configuration directory
codegen.template-config-dir=./codegen_config

# Temporary directory for downloads
codegen.temp-dir=./temp
