package ${base_package}.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class LoggingConfig {
    // Placeholder for logging configuration beans (e.g., MDC setup) if required.
}
